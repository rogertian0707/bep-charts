apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bepchart.fullname" . }}-test-connection"
  labels:
{{ include "bepchart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bepchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
